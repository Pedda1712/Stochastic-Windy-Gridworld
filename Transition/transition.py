# ruff: noqa
"""
State transiton modeling.
"""

from typing import TypeVar, Generic

S = TypeVar("S")
A = TypeVar("A")


class ITransition(Generic[S, A]):

    def get_permissible(self, state: S) -> list[A]:
        """Get permissible actions."""
        return []

    def transition(
        self, states_and_actions: list[tuple[S, A]]
    ) -> list[tuple[S, float]]:
        """
        State Transition function.

        Takes in a list of (State, Action) tuples and returns a list of the same
        size of (State, Reward) tuples.
        """
        return []































































































































































































































"""UGV0ZXIgUHJlaW5lc2JlcmdlciA6KQ=="""  # noqa
